<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.dogetennant.slashserver</groupId>
  <artifactId>SlashServerVelocity</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>SlashServerVelocity</name>
  <description>Per-server slash commands for Velocity proxies.</description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Compiled against the 3.4.0 API on purpose.
         Velocity 4.x ships the exact same api package (verified class-by-class),
         so a jar built here loads on any proxy from 3.4.0 through 4.1.x - which
         is what covers Minecraft 1.21.11 and newer. Bumping this property to a
         4.x API would build fine but would drop support for 3.x proxies. -->
    <velocity.api.version>3.4.0</velocity.api.version>

    <!-- Matches what velocity-api 3.4.0 pulls in. The proxy already has
         Configurate on its classpath, so it is never bundled. -->
    <configurate.version>4.1.2</configurate.version>
  </properties>

  <repositories>
    <!-- Velocity API -->
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
  </repositories>

  <dependencies>

    <!--
         Proxy API  (provided = already on the proxy)

         This also brings in Adventure / MiniMessage and Guice, all of which
         Velocity ships itself - nothing here gets bundled into our jar.
          -->
    <dependency>
      <groupId>com.velocitypowered</groupId>
      <artifactId>velocity-api</artifactId>
      <version>${velocity.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- YAML config loading - Velocity bundles Configurate for plugins -->
    <dependency>
      <groupId>org.spongepowered</groupId>
      <artifactId>configurate-yaml</artifactId>
      <version>${configurate.version}</version>
      <scope>provided</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <!-- Substitutes ${project.version} into BuildConstants.java so the
           @Plugin annotation and the pom can never drift apart. Velocity
           needs a compile-time constant there, so this cannot be a plain
           resource filter the way plugin.yml is on Paper. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>templating-maven-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>filter-src</id>
            <goals>
              <goal>filter-sources</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <source>21</source>
          <target>21</target>
          <encoding>UTF-8</encoding>
          <!-- velocity-api registers an annotation processor that writes
               velocity-plugin.json from our @Plugin annotation. -->
          <annotationProcessorPaths>
            <path>
              <groupId>com.velocitypowered</groupId>
              <artifactId>velocity-api</artifactId>
              <version>${velocity.api.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

    </plugins>

    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <!-- No filtering: config.yml and messages.yml are copied verbatim so
             MiniMessage tags and user text are never touched by Maven. -->
        <filtering>false</filtering>
      </resource>
    </resources>
  </build>

</project>
